
if (Meteor.isServer) {
    Meteor.methods({
        // TODO
    });
}


testAsyncMulti('TODO', (function() {
    if (Meteor.isClient) {
        // TODO
    }

    return [
        function(test, expect) {
            // TODO!
        }
    ];
})());

